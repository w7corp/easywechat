<?php

namespace EasyWeChat\Tests\Pay;

use EasyWeChat\Pay\Merchant;
use EasyWeChat\Pay\Signature;
use EasyWeChat\Tests\TestCase;

class SignatureTest extends TestCase
{
    public function test_it_create_with_get_methods()
    {
//        $merchant = \Mockery::mock(Merchant::class);
//        $merchant->expects()->getMerchantId()->andReturn('merchantId');
//        $merchant->expects()->getPrivateKey()->andReturn('private_key');
//        $merchant->expects()->getCertificateSerialNumber()->andReturn('serial_number');
//        $signature = new Signature($merchant);

        $this->markTestIncomplete();
    }
}
